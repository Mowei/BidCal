# 競拍監控配置範例

## 環境變數

```bash
# 投資目標
TARGET_ROI=30                    # 目標報酬率：30%
MIN_PROFIT=20000                # 最小淨利潤：2 萬元（已扣除手續費）
COMMISSION_DISCOUNT=0.6         # 手續費折扣率：60%

# Email 配置
EMAIL_TO=your.email@gmail.com           # 接收通知的信箱
EMAIL_FROM=your.email@gmail.com         # 寄件信箱（建議用 Gmail）
EMAIL_PASSWORD=xxxx xxxx xxxx xxxx      # Gmail 應用程式密碼（16 碼）

# SMTP 設定（可選）
SMTP_SERVER=smtp.gmail.com       # Gmail SMTP 伺服器
SMTP_PORT=587                    # Gmail SMTP Port
```

## Gmail 應用程式密碼取得步驟

1. **啟用 2 步驟驗證**
   - 進入 Google Account：https://myaccount.google.com
   - 左側選單 → Security
   - 向下滑到 "2-Step Verification"
   - 啟用 2-Step Verification

2. **生成應用程式密碼**
   - 進入 Google App Passwords：https://myaccount.google.com/apppasswords
   - 應用程式選擇：**Mail**
   - 裝置選擇：**Other (custom name)**，輸入「競拍監控」
   - 點擊 Generate
   - 複製 16 碼密碼（包含空格）

3. **在 GitHub 中設置**
   - Repository → Settings → Secrets and variables → Actions
   - 新增 Secret：`EMAIL_PASSWORD`
   - 貼上剛才複製的 16 碼密碼

## 本地測試

```bash
# 設置環境變數
export TARGET_ROI=30
export MIN_PROFIT=20000
export COMMISSION_DISCOUNT=0.6
export EMAIL_TO="your.email@gmail.com"
export EMAIL_FROM="your.email@gmail.com"
export EMAIL_PASSWORD="xxxx xxxx xxxx xxxx"

# 編譯執行
go build -o auction-monitor ./cmd/monitor/main.go
./auction-monitor
```

## Outlook/Hotmail 配置

```bash
EMAIL_TO=your.email@outlook.com
EMAIL_FROM=your.email@outlook.com
SMTP_SERVER=smtp-mail.outlook.com
SMTP_PORT=587
```

## Yahoo Mail 配置

```bash
EMAIL_TO=your.email@yahoo.com
EMAIL_FROM=your.email@yahoo.com
SMTP_SERVER=smtp.mail.yahoo.com
SMTP_PORT=587
```

## ⚠️ 安全提示

- ❌ 不要在代碼中存放密碼
- ✅ 使用 GitHub Secrets 存放敏感信息
- ✅ 使用應用程式專用密碼（而非主密碼）
- ✅ 定期更換密碼和應用程式密碼
